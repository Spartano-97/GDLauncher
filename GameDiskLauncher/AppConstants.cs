namespace GameDiskLauncher
{
    /// <summary> Provides centralized, constant values for the application to prevent "magic strings". </summary>
    public static class AppConstants
    {
        public const string InstallButtonId = "Install";
        public const string StartButtonId = "Start";
        public const string PrimaryStyle = "Primary";
        public const string RestartArgument = "--restarting";
    }
}